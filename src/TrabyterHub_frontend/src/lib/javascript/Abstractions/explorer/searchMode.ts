export enum TokenExplorerSearchMode {
    SearchByTxId = 'SearchByTxId',

    // this is currently not used, because the related code is not implemented yet:
    SearchByPrincipal = 'SearchByPrincipal',
}
